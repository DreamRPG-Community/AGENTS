# DreamRPG 自写插件开发与维护规范

**适用范围:** DreamRPG 服务端中新插件的设计、编码、测试、构建和维护。本文档的首要目标是约束新插件如何复用 `Lib`、如何划分领域责任，以及如何在不破坏已有行为的前提下扩展插件。

**使用方式:** 本文档可以直接提供给 AI 作为 DreamRPG 插件开发约束。新增插件必须遵守全文；维护已有插件时，先保护已经公开的行为，再在本次触及的范围内收敛到本文档的结构和失败策略。

**语言:** 规则使用简体中文。Java、Gradle、包名、类名、方法名、命令和 API 标识符保持英文原样。代码注释、Javadoc 和配置注释使用英文。玩家可见消息可以按产品需要本地化。

## 目录

- [适用范围、优先级与兼容原则](#适用范围优先级与兼容原则)
- [DreamRPG 项目结构与构建基线](#dreamrpg-项目结构与构建基线)
- [Lib 边界与通用逻辑下沉](#lib-边界与通用逻辑下沉)
- [API-first 设计流程](#api-first-设计流程)
- [公共 API、模型与服务](#公共-api模型与服务)
- [注入式生命周期与插件入口](#注入式生命周期与插件入口)
- [依赖、版本与兼容边界](#依赖版本与兼容边界)
- [配置、持久化与线程模型](#配置持久化与线程模型)
- [命令、消息与展示](#命令消息与展示)
- [包结构与代码组织](#包结构与代码组织)
- [AI 执行任务的工作流](#ai-执行任务的工作流)
- [测试、构建与交付检查](#测试构建与交付检查)
- [Java 代码规范](#java-代码规范)
  - [语言与格式](#语言与格式)
  - [Java 链式调用](#java-链式调用)
  - [参数换行](#参数换行)
  - [Java 卫语句](#java-卫语句)
  - [对象构造](#对象构造)
  - [Builder 使用](#builder-使用)
  - [Optional 使用](#optional-使用)
  - [null 与空值](#null-与空值)
  - [空判断与防御代码](#空判断与防御代码)
  - [命名](#命名)
  - [集合与可变性](#集合与可变性)
  - [Java 导入](#java-导入)
  - [注释](#注释)
  - [异常与错误信息](#异常与错误信息)
  - [兜底与失败处理](#兜底与失败处理)
  - [实现选择](#实现选择)
  - [测试代码](#测试代码)
- [例外与提交前审查](#例外与提交前审查)

## 适用范围、优先级与兼容原则

- 用户当前任务的明确要求优先于本规范的通用建议。
- DreamRPG 的当前源码、测试、plugin.yml、构建脚本、配置模板和运行时资源是实现事实。设计结论必须能够被这些材料验证。
- 新插件必须优先遵守本文档。维护已有插件时，先保护已经对玩家、管理员、其他插件和数据文件公开的行为，再在本次触及的范围内收敛规范。
- 未经明确要求，不改变命令、权限、消息、配置键、数据格式、服务契约和兼容边界。
- 不自动迁移、重命名、删除或转换旧配置。明确要求迁移时，必须先设计版本化迁移和回滚策略，并测试失败场景。
- 不使用静默 fallback、空实现、吞异常或默认值掩盖非法状态。
- 需要偏离规则时，在任务说明、代码注释或提交说明中记录原因、影响范围和验证结果。
- 不把密码、Token、私钥、授权头或其他凭据写入源码、配置模板、测试、文档和提交历史。

## DreamRPG 项目结构与构建基线

每个新插件都是独立 Gradle 项目，并通过 `includeBuild("../Lib")` 使用 `Lib`。`Lib` 是独立 Bukkit 插件，也是运行时插件和编译期复用边界；新插件不得复制 Lib 的类或把 Lib 打包进自身 JAR。

推荐结构如下。只创建实际需要的文件和包，不为满足形式而创建空目录。

~~~text
plugins/self-written/
├── Lib/
└── <NewPlugin>/
    ├── build.gradle
    ├── gradle.properties
    ├── settings.gradle
    ├── gradlew
    ├── gradlew.bat
    ├── gradle/
    └── src/
        ├── main/
        │   ├── java/
        │   └── resources/
        └── test/
            └── java/
~~~

DreamRPG 构建基线：

- Paper 1.12.2 API。
- JDK 21。
- Gradle 9.3.0 Wrapper。
- Java release 21，并启用 `-Xlint:all`。
- Bukkit / Paper API 使用编译期依赖，不将服务器 API 打入插件 JAR。
- 插件 JAR 使用稳定名称，不把 Gradle 版本或构建时间写入文件名。
- 生成目录、缓存、IDE 文件、数据库文件、日志和服务器运行时数据不得进入源码提交。
- 配置模板、plugin.yml、迁移脚本和其他运行时资源必须与代码一起审查。

验证命令必须从目标插件目录执行：

~~~text
.\gradlew.bat clean build check --warning-mode all
~~~

## Lib 边界与通用逻辑下沉

`Lib` 是 DreamRPG 自写插件的唯一通用机制库。新插件只负责自己的领域策略、领域模型、业务配置、领域数据和玩家体验；凡是跨插件可复用的机制，必须优先复用或扩展 `Lib`。

依赖方向固定为：

~~~text
DreamRPG 领域插件 / 新业务插件
              ↓
             Lib
~~~

`Lib` 不得依赖任何业务插件，不得理解任何具体业务，不得包含业务插件专属配置键、权限节点、玩家消息、领域事件或数据库表名。

### 必须下沉到 Lib 的能力

能力满足以下任意条件时，不得在新插件中自行实现，应先扩展 `Lib`：

- 被两个或以上自写插件使用，或明显会被后续插件复用。
- 与 Bukkit、Paper、NMS、线程、任务、插件启停、reload 或资源清理相关。
- 属于配置、路径、文本、颜色、Placeholder、Actionbar、命令、数据库、依赖下载、菜单、计分板或文件安全机制。
- 需要统一的主线程约束、异步边界、失败策略、幂等 close 或生命周期清理。
- 包含 Paper 1.12.2 兼容、反射、ClassLoader、原子文件操作或通用编解码。
- 在两个插件中出现第二份相似的 helper、adapter、scheduler、renderer、store 或 parser。

新插件负责的内容只能是：

- DreamRPG 领域术语、业务规则和业务数据。
- 领域 API 的语义、领域模型、领域事件和领域服务。
- 领域配置键、权限节点、玩家消息、数据库表名和业务命令。
- 组合 `Lib` 机制后的领域流程。

### 当前必须优先复用的 Lib 能力

新增插件先检查下列 API 和实现，禁止绕过它们重新手写同类机制：

- 启动与注入：`LibApi`、`PluginBootstrap`、`LibPluginLifecycle`、`ComponentContainer`、`@InjectComponent`、`@ListenerComponent`、`@CommandComponent`、`@ServiceComponent`、`BukkitCommandComponent`。
- 命令：`CommandRouter`、`Subcommand`、`CommandUsageException`、`VanillaCommandMessages`。
- 配置与文本：`ConfigSupport`、`LegacyText`、`TemplateRenderer`、`LegacyActionBar`、`YamlMessageBundle`、`TextAnimation`。
- 任务和玩家加载：`LibPluginLifecycle`、`PlayerLoadingGate`。
- Scoreboard：`ScoreboardService`、`ScoreboardSession`。
- 菜单：`MenuService`、`MenuView`、`StatefulMenuView`、`PageWindow`。
- 存储与文件：`StorageService`、`VersionedPlayerSession`、`AtomicFileStore`、`AtomicYamlStore`、`AtomicYamlTransaction`、`SafePathResolver`、`PathService`、`FileTreeOperations`。
- 数据库与迁移：`DatabaseService`、`SqlDatabase`、`JdbcDatabase`、`SqliteDatabase`、`MigrationRunner`、`MigrationSpec`。
- 运行时依赖：`LibraryService`、`LibrarySpec`、`LibraryLoadResult`。
- 物品与坐标：`ItemStackCodec`、`ItemStackArrayCodec`、`ItemLoreReader`、`LoreAttributeParser`、`LocationSnapper`。
- 外部服务：`PluginLookup`、`VaultEconomyBridge`、`PlayerPointsBridge`、`PlaceholderApiBridge`。
- NMS 容器动画：`ContainerAnimationService`、`ContainerAnimationHandle`、`ContainerAnimationSpec`。

如果 `Lib` 缺少需要的通用机制，先在 `Lib` 设计 API、测试和生命周期契约，再回到业务插件实现领域策略。不得通过插件内部 fallback 掩盖 `Lib` API 不足。

### 新能力的下沉决策树

实现新能力或维护旧能力前，按以下顺序判断：

1. 搜索 `Lib` 的 API、服务、组件和测试，确认没有可复用能力。
2. 判断问题是 `Lib` 机制还是 DreamRPG 领域策略。
3. 如果是机制，先在 `Lib` 设计公共接口、不可变模型、线程契约和生命周期契约。
4. 如果机制需要业务术语，提取中性的技术术语，把业务含义留在消费插件。
5. 在 `Lib` 先编写正常、边界、非法输入和资源清理测试。
6. 实现 `Lib` 能力并补充英文 Javadoc。
7. 同步迁移所有消费者，删除重复实现。
8. 检查主线程、异步线程、关闭顺序、任务取消、资源释放和 ClassLoader 生命周期。
9. 分别构建 `Lib` 和所有受影响的插件。
10. 只有存在清晰领域差异时，才在新插件中保留专属实现。

禁止：

- 先在新插件中复制通用能力，再等待未来抽取。
- 在新插件中直接操作 NMS、反射、ClassLoader、数据库连接、通用文件事务或任务清理，而 `Lib` 已有相应 API。
- 为一个具体领域加入只能由该领域理解的机制到 `Lib`。
- 让 `Lib` 直接引用业务插件的类、配置、事件或数据库表。
- 在多个插件中维护行为略有差异但本质相同的公共实现。
- 使用静默 fallback、空实现或 catch-all 异常隐藏 `Lib` 能力缺失。

### Lib API 的演进

- 公共 API 先定义语义、线程约束、生命周期、成功结果和失败状态。
- 新 API 使用不可变模型、不可变集合和防御性复制。
- 机制 API 不暴露业务配置对象、数据库连接、注入容器、ClassLoader 或内部实现。
- 破坏性 API 变化必须在同一变更中迁移所有消费者并补充测试。
- `Lib` 改动必须构建 `Lib` 和全部受影响插件。
- API、实现和测试必须保持一致，不留下两个长期并存的实现。

## API-first 设计流程

DreamRPG 新插件、新模块、新公共能力和会改变跨插件契约的维护任务必须按以下顺序执行：

1. 读取当前任务、目标源码、测试、插件元数据、构建配置和相关配置资源。
2. 明确领域术语、数据所有权、线程边界、生命周期边界和失败状态。
3. 先设计公开接口、不可变模型、事件和服务注册契约。
4. 为 API 编写编译测试、正常行为测试、边界测试和失败场景测试。
5. 如果能力属于通用机制，先在 `Lib` 实现并验证；如果属于领域策略，再实现插件领域服务、适配器、持久化、命令和监听器。
6. 最后通过 `PluginBootstrap`、配置、任务和 Paper 行为接入插件生命周期。
7. 使用 `Lib` 和目标插件的独立构建，以及必要的 Paper 烟测验证。

维护已有插件时：

- 先识别当前公开行为和兼容约束，再决定 API 是否需要扩展。
- 不把内部 YAML、数据库结构、注入容器或 NMS 类型直接升级为公共 API。
- 不为追求新架构而删除可以继续工作的兼容层。
- 新增功能先建立最小稳定契约，再接入旧实现和旧数据。
- 如果当前实现已经承担多个职责，优先通过稳定接口逐步拆分，不在一次无关任务中重写全部模块。

不得先编写大量实现，再从实现细节反推公共 API。

## 公共 API、模型与服务

- 跨插件服务通过 Bukkit ServicesManager 注册和发现。服务注册、查找和生命周期优先复用 `LibApi` 与 `Lib` 的组件机制。
- 对外暴露只读契约，写入操作默认保持领域内部可见。
- 公开模型优先使用 record 或不可变 final 类。
- 公共集合返回不可变集合、防御性副本或空集合。
- Location、ItemStack、数组和可变配置值必须防御性复制。
- 不把 Optional 作为字段、参数或 Builder 字段。Optional 优先用于返回值。
- 集合类型优先返回空集合，而不是 Optional<List<T>> 或 Optional<Set<T>>。
- 不公开数据库连接、YAML ConfigurationSection、注入容器、ClassLoader、NMS 类型或实现类。
- 公共 API 必须有完整英文 Javadoc，说明目的、参数、返回值、异常、线程约束和生命周期约束。
- 公共事件只表达稳定的领域事实，不隐式承担配置写入、数据库提交或生命周期控制。
- 缺失服务、非法状态、不兼容依赖和违反线程约束必须显式失败。

## 注入式生命周期与插件入口

DreamRPG 已提供 `Lib` 组件扫描、依赖注入和生命周期框架。新插件必须复用该框架，不得在插件入口重新手动组装相同能力。

推荐入口职责：

- 使用 `LibApi.require(this)` 获取 Lib 服务。
- 使用 `LibApi.createPluginBootstrap(this, basePackage)` 创建插件 Bootstrap。
- 调用 enable 和 disable。
- 处理启动和关闭异常。

插件入口不负责手动创建完整业务对象、注册全部监听器、拼接命令树、创建计分板、打开数据库或启动散落任务。

组件通过非私有构造器接收依赖。使用 `@InjectComponent`、`@ListenerComponent`、`@CommandComponent`、`@ServiceComponent` 和 `BukkitCommandComponent`，由 `PluginBootstrap` 统一扫描和生命周期管理。

维护没有注入框架的旧插件时：

- 不为了风格统一而一次性重写全部入口。
- 修改某个生命周期区域时，优先将新增资源纳入现有清理边界。
- 如果维护任务需要迁移旧入口，先设计迁移边界和启动顺序，再逐个迁移组件，不得一次性重写全部启动流程。
- 入口改动必须验证启用失败、重复启用、reload、disable 和异常关闭。

所有任务、监听器、命令、服务、菜单、ScoreboardSession、动画会话、数据库连接和运行时 ClassLoader 必须在关闭时释放。

初始化失败必须停止插件，不得注册半初始化服务，不得让玩家进入部分可用状态。

## 依赖、版本与兼容边界

- 服务器 API 使用编译期依赖，不打入插件 JAR。
- `Lib` 由 Bukkit 依赖图提供，业务插件不得复制或打包一份不一致的 Lib 类。
- 第三方运行时依赖是否打包必须遵循工程的明确依赖策略。未经明确要求，不将 SQLite、MySQL Connector/J 或可选插件 API 随意嵌入插件 JAR。
- 需要运行时下载的依赖必须使用固定坐标、版本和 SHA-256 清单。
- 下载只使用明确配置的唯一仓库，不隐式回退其他来源。
- 缓存存在时必须先校验。下载使用临时文件和原子移动。
- 缺失、下载失败、校验失败、ClassLoader 不兼容或反射初始化失败时必须明确停用相关功能或插件。
- 依赖变更需要完整重启时，reload 不得假装重新加载依赖。
- 可选服务器插件通过 softdepend、服务发现和反射适配，不把可选插件变成无条件硬依赖。
- plugin.yml 的 depend、softdepend、load、命令和权限声明必须与实际生命周期一致。
- NMS、CraftBukkit 和服务器版本专用代码只能隔离在适配层，核心领域代码不得散布版本判断。

## 配置、持久化与线程模型

### 配置

- 通过 `Lib` 的 `ConfigSupport`、模板和校验机制读取配置。
- 当前模板可以补齐当前版本缺失字段，但不得自动迁移旧结构。
- 未知旧键保留并忽略，除非当前任务明确要求清理。
- 非法值必须产生可定位的警告并使用明确记录的处理策略，或直接停用插件。
- 不得用静默默认值掩盖非法配置。
- 配置文件的读取、写入和替换必须遵守路径安全与原子文件规则。
- 配置变更必须说明 reload 是否支持。不能热加载的资源不得在 reload 中伪造成功。

### 持久化

- 不同数据库实现使用同一个领域存储接口。
- SQLite 和 MySQL 等实现必须共享数据语义、版本规则和失败契约。
- 数据库迁移必须版本化、可重复执行、可测试，并在失败时明确停止。
- 数据库表名、编码格式和版本号属于存储契约，变化必须同步迁移和测试。
- 数据加载失败时不得应用部分数据。
- 乐观版本、原子保存、失败回滚和并发冲突必须在存储边界处理。
- ItemStack、Inventory、数组和二进制数据必须使用明确的长度、版本和内容校验。

### 线程

- 数据库、文件、网络和依赖下载 I/O 放在异步线程。
- Bukkit Player、World、Entity、Inventory、Scoreboard、CommandSender 和事件状态只能在主线程访问。
- 异步结果必须切回主线程后才能应用到游戏状态。
- 任务必须记录生命周期归属，并在关闭时取消。
- 不允许异步线程直接操作 Bukkit API，即使当前测试环境暂时没有报错。
- 线程切换必须保留异常，不得通过空回调或无日志的 exceptionally 隐藏失败。

## 命令、消息与展示

- 命令必须拥有明确的根命令、子命令、参数、权限、补全和失败消息。
- 根命令用法和显式 help 用法分离。
- 一个抽象示例是：裸根命令显示简短用法，并提示使用 /plugin help；显式 help 才展开完整子命令列表。
- 参数错误必须显示实际期望格式和失败原因。
- 未知子命令不能吞掉错误，也不能误执行默认业务动作。
- 颜色、模板、Placeholder、Actionbar、TAB、聊天和 Scoreboard 内容必须优先使用 `Lib` 的文本与展示能力。
- Scoreboard 普通刷新必须使用差异更新，不得整体拆除并重建 Scoreboard、Team 或 Score。
- 菜单更新应保持标题、页状态和鼠标位置，避免无意义关闭重开。
- 玩家退出、传送、菜单关闭和插件关闭时，必须清理交互、显示、动画和任务状态。
- 发送区域进入、离开、加载等提示时，应有明确的去重、延迟和替换策略，避免互相覆盖或重复刷屏。

## 包结构与代码组织

- Java 包名使用 `cn.mythicland.<plugin>` 形式。`Lib` 使用 `cn.mythicland.lib`，新插件使用自己的小写标识。
- 使用职责分层加最小固定包结构，不强迫所有插件创建无意义的空包。
- 有公开领域契约时使用 api。
- 有启动和生命周期组件时使用 bootstrap。
- 命令、监听器、配置、存储、集成、展示和领域服务按真实职责分包。
- API 模型不得依赖监听器、命令、数据库或 Bukkit 具体生命周期。
- 领域层不得反向依赖命令层或展示层。
- 集成适配器隔离第三方 API，核心业务不直接散布反射和版本判断。
- 文件、配置、数据库、测试和运行时生成物保持职责边界。
- 不为追求形式统一而增加无意义抽象、空接口、空实现或包装类。

## AI 执行任务的工作流

收到开发或维护任务后，AI 必须按以下顺序工作：

1. 提取用户的目标、限制、不可改变的行为和交付要求。
2. 检查目标模块的源码、测试、插件元数据、构建脚本、配置模板和资源。
3. 搜索 `Lib` 中已有的 API，确认应复用还是扩展。
4. 对维护任务建立行为基线，区分用户要求、当前事实和推测。
5. 先提出或实现公开 API、不可变模型和失败契约。
6. 编写或更新测试，再实现最小范围的代码。
7. 检查线程、生命周期、异常、资源清理和兼容性。
8. 执行目标模块以及受影响模块的构建和测试。
9. 检查 diff、生成物、配置、依赖和敏感信息。
10. 只报告已经验证的结果。未执行的服务器烟测、远程操作或兼容结论不得表述为已完成。

维护任务默认采用最小变更原则：

- 不为了展示新架构而改动无关领域。
- 不删除未知但可能被外部使用的公开 API。
- 不修改玩家可见文本、命令行为、权限和配置结构，除非任务明确要求。
- 不把猜测当成旧实现的事实。
- 如果必须扩大范围，先说明新增影响面和验证计划。

## 测试、构建与交付检查

### 测试要求

- 每个新公共 API 必须有编译测试、正常行为测试、边界测试和失败测试。
- 测试名称、局部变量名和断言必须表达业务含义。
- 配置测试覆盖缺失、非法、边界和未知旧键。
- 存储测试覆盖 SQLite、MySQL 契约、迁移、版本、并发版本和失败回滚。
- `Lib` 改动必须验证所有受影响插件。
- 线程、生命周期、清理和幂等 close 必须有行为测试。
- Paper 兼容和 NMS 反射失败必须有明确失败测试。
- 命令测试覆盖权限、补全、空参数、未知参数和错误消息。
- 展示测试覆盖差异更新、刷新时序、清理和重复调用。
- 不使用隐藏的网络、真实服务器数据或真实凭据作为单元测试前置条件。
- 维护已有插件时，先补充受影响行为的回归测试，再进行结构调整。

### 构建要求

每个独立项目执行：

~~~text
.\gradlew.bat clean build check --warning-mode all
~~~

完成前至少检查：

- Wrapper 版本与项目基线一致。
- Java release 与运行时 JDK 一致。
- -Xlint:all 已启用，新增警告已处理或有明确说明。
- git diff --check 通过。
- 资源文件、plugin.yml 和配置模板正确。
- 依赖、软依赖、API 注册和生命周期一致。
- 无密码、Token、私钥和授权头。
- 失败时插件明确停止或报告，而不是部分启用。

### Paper 烟测

涉及 Bukkit 或 Paper 行为时，至少验证：

- 插件首次启用、重复启用和关闭。
- 缺失依赖、损坏配置和不兼容版本。
- 玩家加入、退出、传送、死亡和重载相关状态。
- 命令、权限、消息、菜单、Scoreboard 和可选集成。
- 异步存储完成后主线程状态正确恢复。
- 所有任务、监听器、服务和临时资源在关闭后不残留。

## Java 代码规范

### 语言与格式

Java 格式化以 IntelliJ IDEA 内置格式为准。构建默认不替代 IDE 格式化检查。

### Java 链式调用

- 对于 ItemBuilder、Component builder、Stream 风格等链式调用，如果链条包含多个步骤，不要压成单行。
- 推荐保留首行创建表达式，后续每个链式方法单独换行，例如：

~~~java
ItemStack lockedSlot = ItemBuilder.of(Material.STAINED_GLASS_PANE)
                .durability(14)
                .name("&cLocked")
                .lore("&7This slot is unavailable.")
                .build();
~~~

- 避免把长链条压缩成横向难以扫描的单行。
- 链式调用中的单个方法如果只有 1 到 2 个短参数，不需要为了参数换行规则再拆开。优先保持链条步骤清晰。

### 参数换行

- 参数换行以可读性优先，而不是只按参数数量机械判断。
- 当方法声明、构造器声明或方法调用的小括号内参数超过 2 个时，默认推荐改为多行。如果类型、变量名和实参都很短，单行仍清晰，则允许保持单行。
- 当任意参数表达式较长、包含 lambda、匿名类、嵌套调用、连续布尔值或 null 等语义不明显的值，或单行可读性明显下降时，应改为多行。
- 多行参数列表中，每个参数单独占一行。
- 方法声明与构造器声明一旦换行，方法名或 new 表达式加 ( 独占首行，之后每个参数各占一行，结尾 ) 单独一行并与声明开头对齐。
- 方法调用换行没有“必须每个参数一行”的硬性要求。调用点以可读性优先，短参数可以多个同行。
- 如果规则之间冲突，以“短且清晰可单行，长或复杂才多行”为准。

例如两个短参数的方法调用推荐保持紧凑：

~~~java
String skinName = section.getString(internalName, "").trim();
ItemStack itemStack = this.resolveItem(event, player);
if (!this.canUse(player, itemId)) return;
~~~

短类型、短变量名的声明即使超过两个参数，也可以保持单行：

~~~java
private int clamp(int value, int min, int max) {
~~~

参数较长或语义不容易扫描时改成多行：

~~~java
DamageEvent event = new DamageEvent(
        attacker,
        target,
        DamageRequest.builder()
                .sourceType(SourceType.PLAYER)
                .damageType(DamageType.DEFAULT)
                .build()
);
~~~

方法声明一旦换行，必须每个参数单独一行：

~~~java
public static ArrayList<Vec3> computePath(
        final Vec3 startVec3,
        final Vec3 endVec3,
        final HashSet<Long> excludedKeys
) {
~~~

反例：

~~~java
public static ArrayList<Vec3> computePath(final Vec3 startVec3, final Vec3 endVec3,
        final HashSet<Long> excludedKeys) {
~~~

方法调用换行则没有每个参数一行的硬性要求，短参数可以同行：

~~~java
final PathNode startNode = new PathNode(
        start.getX(), start.getY(), start.getZ(),
        heuristic(start.getX(), start.getY(), start.getZ())
);
~~~

### Java 卫语句

- 对于简单的提前返回卫语句，推荐省略大括号并写成单行：

~~~java
if (player == null) return;
if (implementationType == null) return null;
~~~

- 适用范围限于短的 return、continue、break 和 throw 控制流。
- 如果条件或返回表达式较长、包含注释、存在 else 或单行后可读性下降，则保留多行大括号。
- 长的复合卫语句通常优先拆成多个短卫语句。只有当多个条件共同表达同一个业务概念时，才提取具名方法或具名布尔变量。

多个独立条件推荐拆开：

~~~java
if (user == null) return;
if (!user.isActive()) return;
if (!permissionService.canAccess(user, resource)) return;
~~~

如果复合条件表达一个整体业务概念，可以提取具名方法：

~~~java
if (!canAccessResource(user, resource)) return;
~~~

- 编写新代码或修改旧代码时，优先使用卫语句减少不必要的嵌套，但不要为了形式统一强行重构清晰的业务分支。
- 当 if/else 表达成功/失败、允许/拒绝、命中/未命中等对称业务分支时，可以保留分支结构。业务语义清晰优先于形式上的卫语句化。

### 对象构造

- 当构造器、record 或静态工厂的参数超过 5 到 6 个，尤其出现连续的 true、false、0、null 等难以读出含义的参数时，优先考虑命名静态工厂、参数对象或 Builder。
- 不为了追求模式统一而给简单对象增加额外抽象。只有当命名能明显改善调用点可读性时再引入。
- 当构造器、静态工厂或 this(...) 委托参数超过 6 个，且混有多个同类型参数、函数式参数、数字常量或可选行为时，不应继续堆叠新的重载链。
- 命名静态工厂应表达业务场景，而不是只复述参数形状，例如 singleLevel(...)、scalableWithFixedUpgradeCost(...)、scalableWithLevelCost(...)。
- 多个重载委托到核心构造器时，避免直接传递含义不明显的默认值、null、0、20 或无名 lambda。可以使用局部变量或具名方法说明语义。
- 当参数可能同时被理解为首级价格、升级价格或每级价格时，必须拆分命名，例如 firstLevelRenown、upgradeRenown、requiredRenownByLevel。
- 对于可选行为，优先使用空对象或具名默认方法表达默认值，例如 DamageEffect.none()、TickEffect.none()。如果默认值会影响价格、等级、权限或消耗材料，应通过命名工厂或参数对象集中表达。

### Builder 使用

- Builder 主要用于字段较多、可选参数较多、构造语义复杂的对象。
- 不为了统一风格给简单对象强行增加 Builder。
- 对于只有少量字段、语义清晰的对象，优先使用构造器、record 或静态工厂。
- Builder 的 build() 方法负责必要校验，避免创建非法对象。
- 如果必填字段很多，优先考虑构造器、静态工厂、参数对象或 staged builder 来表达必填约束。

### Optional 使用

- Optional 推荐用于方法返回值，用来表达可能没有结果。
- 不推荐把 Optional 作为字段类型、方法参数类型或 Builder 字段类型。
- 集合类型优先返回空集合，而不是 Optional<List<T>>、Optional<Set<T>>。
- 避免直接使用 optional.get()。优先使用 orElse、orElseGet、orElseThrow 或显式分支。
- 当缺失值属于异常情况时，使用 orElseThrow() 并提供清晰的异常信息。

### null 与空值

- 方法返回集合或数组时，优先返回空集合或空数组，而不是 null。
- 只有当 null 具有明确业务语义时，才允许返回或传递 null。
- 对外暴露的 API 如果可能返回 null，应通过注解、文档或方法命名明确表达。
- 对于可选行为，优先使用具名默认值、空对象或默认实现，而不是散落的 null。
- 避免在长参数列表中直接传递多个 null，应改用参数对象、Builder 或具名变量说明含义。

### 空判断与防御代码

- 避免为了保险添加无意义的 null、空集合、空字符串判断。
- 已由上游校验、构造器约束、注解或生命周期保证的值，不需要重复防御。
- 空判断应对应明确业务分支、兼容边界或外部输入边界。
- 对不应发生的状态，优先显式失败，而不是静默返回或继续执行。
- 连续多层空判断导致主流程不清晰时，优先收敛校验入口或拆出具名方法。

### 命名

- 命名优先表达业务含义，而不是类型或实现细节。
- 避免使用过于宽泛的名称，例如 data、info、obj、temp、manager。
- 布尔变量和布尔方法优先使用 is、has、can、should、allow 等前缀表达语义。
- 避免使用否定布尔名，例如 notFound、notAllowed，除非它本身就是业务概念。
- 涉及数量、时间、大小、距离、金额等值时，变量名应尽量体现单位或语义，例如 timeoutMillis、durationSeconds、maxRetries。

### 集合与可变性

- 对外暴露集合时，避免直接返回内部可变集合。
- 如果调用方不应该修改返回结果，优先返回不可变集合、不可变视图或防御性副本。
- 方法返回集合时，优先返回空集合，而不是 null。
- 不要假设调用方不会修改传入的集合。必要时应复制一份再保存。
- 对于不会变化的常量集合，优先使用不可变集合表达意图。

### Java 导入

- 尽量避免在正文代码中直接书写全限定名类型声明。
- 优先通过 import 导入类型后再直接使用简单类名。
- 只有在确实需要消除同名类型冲突，或局部使用全限定名能明显降低歧义时，才保留全限定名写法。

### 注释

- 注释优先解释为什么这样做，而不是重复代码做了什么。
- 对于临时兼容、历史遗留、特殊边界和性能折中，应写明原因。
- 如果代码需要大量注释才能读懂，优先考虑改命名、拆方法或调整结构。
- TODO 应写明后续动作，避免只写 TODO fix。

### 异常与错误信息

- 异常信息应包含足够定位问题的上下文。
- 不要吞掉异常。如果确实需要忽略，应通过注释说明原因。
- 捕获异常时，优先捕获具体异常类型，避免无意义地捕获 Exception。
- 抛出 IllegalArgumentException、IllegalStateException 等异常时，信息应说明哪个参数或状态不合法。
- 不要在异常信息中泄露密码、Token、密钥等敏感信息。

### 兜底与失败处理

- 避免写无明确业务语义的默认值、默认分支或 catch-all 兜底。
- 缺失配置、未知类型、非法状态和未支持分支，优先抛出带上下文的异常。
- 只有当业务明确允许降级、跳过或默认处理时，才写兜底逻辑。
- 兜底逻辑应可定位原因，必要时记录上下文或集中处理。
- 不用空实现、空返回和默认对象掩盖数据错误或流程错误。

### 实现选择

- 优先使用 JDK、项目已有工具类或成熟依赖提供的稳定实现。
- 避免为常见能力重复手写不完整实现。
- 避免使用实验性、临时性或行为不稳定的实现作为核心路径。
- 当简单直接的实现已经足够清晰时，不为了可扩展而提前引入复杂抽象。
- 新增抽象应服务于真实重复、清晰边界或可测试性。

### 测试代码

- 测试名、局部变量名和断言应尽量表达业务含义，避免只留下无法解释意图的魔法值。
- 当断言使用固定数字、字符串或物品数量时，优先用变量名或辅助方法说明为什么是这个值。
- 测试失败消息应说明业务期望，而不是只显示内部实现细节。
- 测试必须验证可观察行为和失败边界，不只验证私有方法的当前写法。

## 例外与提交前审查

### 可接受例外

- Paper 或 NMS 反射需要保留版本专用代码时，必须隔离在适配层，并说明兼容原因。
- 第三方 API 的限制迫使某个插件保留适配器时，适配器必须与领域核心隔离，并有失败测试。
- 历史数据格式无法直接抽象时，可以保留领域专属迁移，但必须显式版本化。
- 性能优化需要跳过通用封装时，必须说明基准、线程安全和资源清理影响。
- 维护任务必须保留现有兼容行为时，可以暂时保留不符合新结构的边界，但必须限制影响面并避免复制扩散。

### 提交前审查

- 是否明确区分了用户要求、当前事实和推测？
- 是否先检查了 `Lib` 是否已经提供相同机制？
- 新 API 是否先于实现设计？
- 公共模型是否不可变，集合和可变值是否防御性复制？
- 是否暴露了实现类、数据库、配置、注入容器或 NMS？
- 插件入口是否只负责启动和关闭，或已有入口是否避免了无关重构？
- 是否存在重复的命令、任务、文本、文件、数据库或生命周期实现？
- 是否所有异步操作都避开 Bukkit API？
- 是否所有任务、监听器、菜单、Scoreboard、数据库和临时资源都能清理？
- 是否没有自动迁移、重命名或删除旧配置？
- 是否所有非法状态都会显式失败？
- 是否补充了正常、边界、失败和回归测试？
- 是否使用项目声明的 Java、服务器 API 和 Gradle Wrapper 构建通过？
- 是否通过 git diff --check？
- 是否没有密码、Token、私钥或授权头？
- 是否只报告了已经实际验证的结果？
